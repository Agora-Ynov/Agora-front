import { Component } from '@angular/core';

/** Réutilisé uniquement pour la config de route `/admin` : la garde redirige avant affichage. */
@Component({
  standalone: true,
  selector: 'app-admin-root-stub',
  template: '',
})
export class AdminRootStubComponent {}
